spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/gpu_management_db
    username: diego
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
